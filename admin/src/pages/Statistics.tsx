import type React from 'react';

const Roles: React.FC = () => {
  return (
    <div>
      <h2 className='text-2xl font-bold mb-4'>Roles & Permissions</h2>
      {/* Add role and permission management content here */}
    </div>
  );
};

export default Roles;
